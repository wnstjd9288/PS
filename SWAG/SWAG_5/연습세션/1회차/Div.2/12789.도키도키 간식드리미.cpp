#include <bits/stdc++.h>

int main()
{
    int arr[1001];
    int n;
    int inverse = 0;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &arr[i]);
        if (i&&arr[i])
    }
}