#include <bits/stdc++.h>

int main()
{
    int t;
    scanf("%d", &t);
    for (int test = 0; test < t; test++)
    {
        int n;
        int arr[8001], c[8001];
        scanf("%d", &n);
        for (int i = 0; i < n; i++)
            scanf("%d", &arr[i]);
        for (int i = 0; i < n; i++)
            scanf("%d", &c[i]);
        
    }
}